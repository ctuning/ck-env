#!/bin/bash


if [ -z ${CK_ENV_DATASET_COCO_TRAIN_TRAIN_IMAGE_DIR+"test"} ]; then
  IMAGE_DIR="${CK_ENV_DATASET_IMAGE_DIR}"
else
  IMAGE_DIR="${CK_ENV_DATASET_IMAGE_DIR}/${CK_ENV_DATASET_COCO_TRAIN_TRAIN_IMAGE_DIR}"
fi

${CK_ENV_COMPILER_PYTHON_FILE} "${PACKAGE_DIR}/preprocess_image_dataset.py" "${IMAGE_DIR}" "${INSTALL_DIR}"
